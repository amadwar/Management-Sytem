import { ChangeDetectionStrategy, Component, OnInit } from '@angular/core';import { FormControl, FormGroup, ReactiveFormsModule } from '@angular/forms';import { ApiService } from '../../core/api/api.service';@Component({selector:'app-settings',imports:[ReactiveFormsModule],template:`<h1>Company Settings</h1><form class="card login-card" [formGroup]="form" (ngSubmit)="save()"><label>Default language<select formControlName="default_language"><option value="en">English</option><option value="ar">العربية</option><option value="de">Deutsch</option></select></label><label>Timezone<input formControlName="timezone"></label><button class="primary">Save settings</button></form>`,changeDetection:ChangeDetectionStrategy.OnPush})export class SettingsComponent implements OnInit{readonly form=new FormGroup({default_language:new FormControl('en',{nonNullable:true}),timezone:new FormControl('UTC',{nonNullable:true})});constructor(private api:ApiService){}ngOnInit(){this.api.get<{data:Record<string,unknown>}>('/settings').subscribe(r=>this.form.patchValue({default_language:String(r.data['default_language']??'en'),timezone:String(r.data['timezone']??'UTC')}));}save(){this.api.put('/settings',{settings:this.form.getRawValue()}).subscribe();}}