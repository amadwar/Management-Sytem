import { ChangeDetectionStrategy, Component, OnInit, signal } from '@angular/core';import { FormControl, FormGroup, ReactiveFormsModule, Validators } from '@angular/forms';import { ApiService } from '../../core/api/api.service';interface Plan{public_id:string;name:string;code:string;monthly_price:string;annual_price:string;currency_code:string;is_active:boolean}@Component({selector:'app-platform-plans',imports:[ReactiveFormsModule],template:`<main class="content"><div class="page-head"><div><h1>Subscription Plans</h1><p>Billing foundation for future plan limits and subscriptions.</p></div><a href="/platform/tenants">Tenants</a></div><div class="grid platform-grid"><form class="card login-card" [formGroup]="form" (ngSubmit)="create()"><h3>Create plan</h3><label>Name<input formControlName="name"></label><label>Code<input formControlName="code"></label><label>Monthly<input type="number" formControlName="monthly_price"></label><label>Annual<input type="number" formControlName="annual_price"></label><label>Currency<input formControlName="currency_code"></label><button class="primary">Create</button></form><div class="card"><table><thead><tr><th>Name</th><th>Monthly</th><th>Annual</th><th>Status</th></tr></thead><tbody>@for(p of plans();track p.public_id){<tr><td>{{p.name}}</td><td>{{p.monthly_price}} {{p.currency_code}}</td><td>{{p.annual_price}} {{p.currency_code}}</td><td>{{p.is_active?'Active':'Inactive'}}</td></tr>}</tbody></table></div></div></main>`,changeDetection:ChangeDetectionStrategy.OnPush})export class PlatformPlansComponent implements OnInit{readonly plans=signal<Plan[]>([]);readonly form=new FormGroup({name:new FormControl('',{nonNullable:true,validators:[Validators.required]}),code:new FormControl('',{nonNullable:true,validators:[Validators.required]}),monthly_price:new FormControl(0,{nonNullable:true}),annual_price:new FormControl(0,{nonNullable:true}),currency_code:new FormControl('USD',{nonNullable:true})});constructor(private api:ApiService){}ngOnInit(){this.load();}load(){this.api.get<{data:Plan[]}>('/platform/plans').subscribe(r=>this.plans.set(r.data));}create(){if(this.form.invalid)return;this.api.post('/platform/plans',this.form.getRawValue()).subscribe(()=>{this.form.reset({name:'',code:'',monthly_price:0,annual_price:0,currency_code:'USD'});this.load();});}}