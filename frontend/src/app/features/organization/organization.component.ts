import { ChangeDetectionStrategy, Component, OnInit, signal } from '@angular/core';import { ApiService } from '../../core/api/api.service';interface Organization{id:string;legal_name:string;display_name:string;email?:string;phone?:string;tax_number?:string;address?:string}@Component({selector:'app-organization',template:`<h1>Organization</h1>@if(org();as o){<div class="card detail-grid"><div><span>Legal name</span><strong>{{o.legal_name}}</strong></div><div><span>Display name</span><strong>{{o.display_name}}</strong></div><div><span>Email</span><strong>{{o.email||'-'}}</strong></div><div><span>Phone</span><strong>{{o.phone||'-'}}</strong></div></div>}@else{<div class="card">Loading...</div>}`,changeDetection:ChangeDetectionStrategy.OnPush})export class OrganizationComponent implements OnInit{readonly org=signal<Organization|null>(null);constructor(private api:ApiService){}ngOnInit(){this.api.get<{data:Organization}>('/organization').subscribe(r=>this.org.set(r.data));}}