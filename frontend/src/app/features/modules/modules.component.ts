import { ChangeDetectionStrategy, Component, OnInit, signal } from '@angular/core';import { ApiService } from '../../core/api/api.service';interface ModuleRow{code:string;name:string;description?:string;is_core:boolean;enabled:boolean}@Component({selector:'app-modules',template:`<h1>Modules</h1><p>Enable business capabilities per tenant.</p><div class="grid">@for(module of modules();track module.code){<article class="card"><div class="module-title"><h3>{{module.name}}</h3><span class="badge">{{module.enabled?'Enabled':'Disabled'}}</span></div><p>{{module.description}}</p><small>{{module.is_core?'Core module':'Optional module'}}</small></article>}</div>`,changeDetection:ChangeDetectionStrategy.OnPush})export class ModulesComponent implements OnInit{readonly modules=signal<ModuleRow[]>([]);constructor(private api:ApiService){}ngOnInit(){this.api.get<{data:ModuleRow[]}>('/modules').subscribe(r=>this.modules.set(r.data));}}