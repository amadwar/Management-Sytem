import { ChangeDetectionStrategy, Component, OnInit, signal } from '@angular/core';import { ApiService } from '../../core/api/api.service';interface User{id:string;name:string;email:string;status:string}@Component({selector:'app-users',template:`<div class="page-head"><div><h1>Users</h1><p>Tenant-scoped company accounts.</p></div></div><div class="card"><table><thead><tr><th>Name</th><th>Email</th><th>Status</th></tr></thead><tbody>@for(user of users();track user.id){<tr><td>{{user.name}}</td><td>{{user.email}}</td><td>{{user.status}}</td></tr>}@empty{<tr><td colspan="3">No users.</td></tr>}</tbody></table></div>`,changeDetection:ChangeDetectionStrategy.OnPush})export class UsersComponent implements OnInit{readonly users=signal<User[]>([]);constructor(private api:ApiService){}ngOnInit(){this.api.get<{data:User[]}>('/users').subscribe(r=>this.users.set(r.data));}}