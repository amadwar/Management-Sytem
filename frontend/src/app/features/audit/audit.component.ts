import { ChangeDetectionStrategy, Component, OnInit, signal } from '@angular/core';import { ApiService } from '../../core/api/api.service';interface Audit{id:number;action:string;auditable_type?:string;auditable_id?:string;ip_address?:string;created_at:string}@Component({selector:'app-audit',template:`<h1>Audit Log</h1><div class="card"><table><thead><tr><th>Action</th><th>Target</th><th>IP</th><th>Time</th></tr></thead><tbody>@for(row of rows();track row.id){<tr><td>{{row.action}}</td><td>{{row.auditable_type||'-'}} {{row.auditable_id||''}}</td><td>{{row.ip_address||'-'}}</td><td>{{row.created_at}}</td></tr>}@empty{<tr><td colspan="4">No audit entries.</td></tr>}</tbody></table></div>`,changeDetection:ChangeDetectionStrategy.OnPush})export class AuditComponent implements OnInit{readonly rows=signal<Audit[]>([]);constructor(private api:ApiService){}ngOnInit(){this.api.get<{data:Audit[]}>('/audit-logs').subscribe(r=>this.rows.set(r.data));}}