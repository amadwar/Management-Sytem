import { ChangeDetectionStrategy, Component, OnInit, signal } from '@angular/core';import { ApiService } from '../../core/api/api.service';interface Role{id:string;name:string;code:string;is_system:boolean}@Component({selector:'app-roles',template:`<h1>Roles & Permissions</h1><div class="grid">@for(role of roles();track role.id){<article class="card"><h3>{{role.name}}</h3><code>{{role.code}}</code><p>{{role.is_system?'System role':'Custom role'}}</p></article>}@empty{<div class="card">No roles.</div>}</div>`,changeDetection:ChangeDetectionStrategy.OnPush})export class RolesComponent implements OnInit{readonly roles=signal<Role[]>([]);constructor(private api:ApiService){}ngOnInit(){this.api.get<{data:Role[]}>('/roles').subscribe(r=>this.roles.set(r.data));}}