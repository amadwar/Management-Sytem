import { Injectable, computed, signal } from '@angular/core';import { HttpClient } from '@angular/common/http';import { Router } from '@angular/router';import { tap } from 'rxjs';import { environment } from '../../../environments/environment';import { CurrentUser } from './auth.service';@Injectable({providedIn:'root'})export class PlatformAuthService{private readonly tokenKey='mdr_platform_token';readonly user=signal<CurrentUser|null>(null);readonly authenticated=computed(()=>!!localStorage.getItem(this.tokenKey));constructor(private http:HttpClient,private router:Router){}token(){return localStorage.getItem(this.tokenKey);}login(payload:{email:string;password:string}){return this.http.post<{data:{token:string;user:CurrentUser}}>(`${environment.apiUrl}/platform/auth/login`,payload).pipe(tap(r=>{localStorage.setItem(this.tokenKey,r.data.token);this.user.set(r.data.user);}));}loadMe(){return this.http.get<{data:CurrentUser}>(`${environment.apiUrl}/platform/auth/me`).pipe(tap(r=>this.user.set(r.data)));}logout(){return this.http.post(`${environment.apiUrl}/platform/auth/logout`,{}).pipe(tap(()=>{localStorage.removeItem(this.tokenKey);this.user.set(null);this.router.navigateByUrl('/platform/login');}));}}